/**
 * Stub for E1-T9. Requests a screen wake lock while `active` is true, releases it on `false` or
 * unmount; an absent `navigator.wakeLock` or a rejected request is a silent no-op.
 */
export function useWakeLock(_active: boolean): void {
  // Not yet implemented.
}
