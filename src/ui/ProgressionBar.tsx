import './ProgressionBar.css'
import type { Progression } from '../domain/progression'

export type ProgressionBarProps = { progression: Progression }

/** E4-T6 stub: the red commit's placeholder, replaced by the implementation. */
export function ProgressionBar(_props: ProgressionBarProps): JSX.Element {
  return <></>
}
