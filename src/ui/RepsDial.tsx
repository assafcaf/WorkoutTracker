export type RepsDialProps = {
  value: number
  onChange(value: number): void
}

/**
 * Stub for E1-T5. The reps scroll-snap column with its minus/plus buttons and its readout.
 */
export function RepsDial(_props: RepsDialProps): JSX.Element {
  return <div />
}
