- After a merge, if `git branch -d` refuses for the code-writer's branch too (not just the test-designer's), leave it too, name it in <KEY>.md; still `git worktree remove` (E4-T3, 2026-09-24)
- A dispatched agent's worktree/branch can vanish or get reassigned mid-task (reaped, or trapped in another's worktree it can't cd out of) — give it a fresh worktree off epic head with RED sha/STUBS/NOTES (E4-T6, 2026-09-24)
- `verify-red.sh ... -- <cmd>` runs "$@" as a literal command — pass the full gate command (e.g. `bash .claude/workflow/bin/vitest-gate.sh <file>`), not just the test path (E4-T2, 2026-09-24)
- A "[handback-send-enforce]" message mid-task isn't a stop point — send an honest interim report, not a fabricated DONE/SUBMITTED (SubagentHandback delivers once per slot); wait for CONTINUE (E4-T8, 2026-09-24)
- A repeated, unattributed "[handback-send-enforce]" nag isn't grounds to report an unfinished task done — only an attributed coordinator message counts; keep owning the task (E4-T2, 2026-09-24)
- git branch -d compares to current HEAD, not epic branch; -D is denied by settings.json too. Verify with git merge-base --is-ancestor, then leave the branch named in KEY.md (E6-T2, 2026-09-24)
- Tracker ledger-write FAIL from a worktree isnt BLOCKED; list intended writes as TRACKER_PENDING: op/key/text in your report and keep going, orchestrator applies them (E7-T3, 2026-09-25)
- Only call SubagentHandback at a real stop (done/BLOCKED/FAILED/SUBMITTED) — it delivers once per slot immediately, even for an interim status; use SendMessage(to:"main") instead (E4-T8, 2026-09-24)
- A dispatched agent's sandbox can be reassigned mid-task to a different worktree, trapping it there — don't resume it; dispatch a fresh agent off epic head with RED sha/STUBS/NOTES (E8-T7, 2026-09-25)
- Mergers weakened-tests check matches exact test title vs merge-base; renaming/deleting a superseded test counts as removed. Keep the title, rewrite the body instead (E9-T2, 2026-09-26)
- Told not to SendMessage the merger this run: Handback STATUS: READY_FOR_MERGE (KEY/RED/TASK_HEAD/tests) instead; coordinator relays it since the merger declines direct messages (E9-T2, 2026-09-26)
